# Importamos la clase MySQL desde la librería Flask-MySQLdb
from flask_mysqldb import MySQL

# Creamos una instancia de la clase MySQL
# La inicializaremos más tarde en app.py con la configuración
mysql = MySQL()